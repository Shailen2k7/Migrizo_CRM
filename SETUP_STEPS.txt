MIGRIZO CRM — ROADMAP MODULE · DEPLOY
======================================
REQUIRES: Email module already deployed (it uses lead_emails + your signature).

STEP 1 · SUPABASE (30 sec)
  SQL Editor → run: supabase/migrations/016_roadmaps.sql

STEP 2 · GITHUB (one commit) — 3 NEW files + 1 replacement
  NEW files — create each via GitHub "Add file → Create new file",
  typing the FULL path (folders auto-create). Do NOT drag these:
    lib/roadmap/types.ts
    lib/roadmap/template.ts
    app/api/roadmap/send/route.ts
    components/roadmap/roadmap-tab.tsx
  REPLACEMENT — drag into its existing folder:
    components/leads/lead-drawer.tsx
  Commit. Wait for Netlify green.

STEP 3 · USE IT (per client, ~3 minutes)
  a) Claude (Max) → new chat → attach the Evidence Playbook →
     paste client's Drive link + the prompt from ROADMAP_PROMPT.txt
     (set the number of weeks in the prompt).
  b) Copy the JSON Claude returns.
  c) CRM → lead → Roadmap tab → paste → Parse & preview.
  d) Edit anything inline (weeks: edit / delete / add / reorder).
  e) Preview & send → Confirm. It emails the branded roadmap and logs
     it in the lead's Emails thread. Or Download PDF (print dialog →
     Save as PDF) for WhatsApp.

RULE OF THUMB (built into the UI as a reminder):
  Timeline changes (8→4 weeks) = re-ask Claude, it re-prioritises.
  Delete button = only for tasks that don't apply to this client.
